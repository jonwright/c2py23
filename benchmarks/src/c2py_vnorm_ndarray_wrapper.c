/* Generated by c2py23 - do not edit by hand */
#include <stdio.h>
#include "c2py_runtime.h"
#include "tiny_kernel.h"

extern void vnorm(const double (*vec)[3], double mods[], int n);

static const uint8_t _acqord_vnorm[] = { C2PY_PIN_NDARRAY };

/* -------------------------------------------- */
/* Wrapper for: vnorm */
/* -------------------------------------------- */

static PyObject*
_vnorm_impl(c2py_ptr_info *info_vec, c2py_ptr_info *info_mods)
{
    int _c2py_slow_axis_info_vec = -1;
    int _c2py_fast_axis_info_vec = -1;
    (void)_c2py_slow_axis_info_vec;
    (void)_c2py_fast_axis_info_vec;
    /* contiguity check: vec */
    do {
        int _ok = 1;
        if (info_vec->strides == NULL && info_vec->ndim <= 1) {
            _c2py_slow_axis_info_vec = 0;
            _c2py_fast_axis_info_vec = (int)(info_vec->ndim - 1);
            break;
        }
        if (info_vec->ndim >= 1) {
            Py_ssize_t _expected = info_vec->itemsize;
            int _d;
            /* check F-contiguous (column-major): first dim varies fastest */
            for (_d = 0; _d < info_vec->ndim; _d++) {
                if (info_vec->strides[_d] < 0) { _ok = 0; break; }
                if (info_vec->strides[_d] != _expected) { _ok = 0; break; }
                _expected *= info_vec->shape[_d];
            }
            if (_ok) { _c2py_slow_axis_info_vec = (int)(info_vec->ndim - 1); _c2py_fast_axis_info_vec = 0; break; }
            /* check C-contiguous (row-major): last dim varies fastest */
            _ok = 1;
            _expected = info_vec->itemsize;
            for (_d = info_vec->ndim - 1; _d >= 0; _d--) {
                if (info_vec->strides[_d] < 0) { _ok = 0; break; }
                if (info_vec->strides[_d] != _expected) { _ok = 0; break; }
                _expected *= info_vec->shape[_d];
            }
            if (_ok) { _c2py_slow_axis_info_vec = 0; _c2py_fast_axis_info_vec = (int)(info_vec->ndim - 1); }
        }
        if (!_ok) {
            PyErr_SetString(PyExc_ValueError,
                "buffer not contiguous (C or Fortran contiguous required)");
            return NULL;
        }
    } while(0);

    int _c2py_slow_axis_info_mods = -1;
    int _c2py_fast_axis_info_mods = -1;
    (void)_c2py_slow_axis_info_mods;
    (void)_c2py_fast_axis_info_mods;
    /* contiguity check: mods */
    do {
        int _ok = 1;
        if (info_mods->strides == NULL && info_mods->ndim <= 1) {
            _c2py_slow_axis_info_mods = 0;
            _c2py_fast_axis_info_mods = (int)(info_mods->ndim - 1);
            break;
        }
        if (info_mods->ndim >= 1) {
            Py_ssize_t _expected = info_mods->itemsize;
            int _d;
            /* check F-contiguous (column-major): first dim varies fastest */
            for (_d = 0; _d < info_mods->ndim; _d++) {
                if (info_mods->strides[_d] < 0) { _ok = 0; break; }
                if (info_mods->strides[_d] != _expected) { _ok = 0; break; }
                _expected *= info_mods->shape[_d];
            }
            if (_ok) { _c2py_slow_axis_info_mods = (int)(info_mods->ndim - 1); _c2py_fast_axis_info_mods = 0; break; }
            /* check C-contiguous (row-major): last dim varies fastest */
            _ok = 1;
            _expected = info_mods->itemsize;
            for (_d = info_mods->ndim - 1; _d >= 0; _d--) {
                if (info_mods->strides[_d] < 0) { _ok = 0; break; }
                if (info_mods->strides[_d] != _expected) { _ok = 0; break; }
                _expected *= info_mods->shape[_d];
            }
            if (_ok) { _c2py_slow_axis_info_mods = 0; _c2py_fast_axis_info_mods = (int)(info_mods->ndim - 1); }
        }
        if (!_ok) {
            PyErr_SetString(PyExc_ValueError,
                "buffer not contiguous (C or Fortran contiguous required)");
            return NULL;
        }
    } while(0);

    /* check: vec.format == 'd' */
    if (!((!info_vec->format || info_vec->format[strlen(info_vec->format) - 1] == 'd'))) {
        char _c2py_err[256];
        const char *_fmt = info_vec->format ? info_vec->format : "";
        char _got = _fmt[0] ? _fmt[strlen(_fmt) - 1] : '?';
        snprintf(_c2py_err, sizeof(_c2py_err), "check failed: vec.format == 'd' (got format='%c')", _got);
        PyErr_SetString(PyExc_ValueError, _c2py_err);
        return NULL;
    }
    /* check: mods.format == 'd' */
    if (!((!info_mods->format || info_mods->format[strlen(info_mods->format) - 1] == 'd'))) {
        char _c2py_err[256];
        const char *_fmt = info_mods->format ? info_mods->format : "";
        char _got = _fmt[0] ? _fmt[strlen(_fmt) - 1] : '?';
        snprintf(_c2py_err, sizeof(_c2py_err), "check failed: mods.format == 'd' (got format='%c')", _got);
        PyErr_SetString(PyExc_ValueError, _c2py_err);
        return NULL;
    }
    /* check: vec.ndim == 2 */
    if (!((info_vec->ndim) == (2))) {
        char _c2py_err[256];
        snprintf(_c2py_err, sizeof(_c2py_err), "check failed: vec.ndim == 2 (got %ld vs %ld)", (long)(info_vec->ndim), (long)(2));
        PyErr_SetString(PyExc_ValueError, _c2py_err);
        return NULL;
    }
    /* check: mods.ndim == 1 */
    if (!((info_mods->ndim) == (1))) {
        char _c2py_err[256];
        snprintf(_c2py_err, sizeof(_c2py_err), "check failed: mods.ndim == 1 (got %ld vs %ld)", (long)(info_mods->ndim), (long)(1));
        PyErr_SetString(PyExc_ValueError, _c2py_err);
        return NULL;
    }
    /* check: vec.shape[1] == 3 */
    if (!((info_vec->shape[1]) == (3))) {
        PyErr_SetString(PyExc_ValueError, "check failed: vec.shape[1] == 3");
        return NULL;
    }
    /* check: mods.n == vec.shape[0] */
    if (!((((info_mods->len == 0) ? 0 : (info_mods->len / info_mods->itemsize))) == (info_vec->shape[0]))) {
        PyErr_SetString(PyExc_ValueError, "check failed: mods.n == vec.shape[0]");
        return NULL;
    }
    /* check: vec.slow_axis == 0 */
    if (!((_c2py_slow_axis_info_vec) == (0))) {
        char _c2py_err[256];
        snprintf(_c2py_err, sizeof(_c2py_err), "check failed: vec.slow_axis == 0 (got %ld). Buffer must be C-contiguous (use slow_axis=0 or [][] notation).", (long)(_c2py_slow_axis_info_vec));
        PyErr_SetString(PyExc_ValueError, _c2py_err);
        return NULL;
    }
    /* check: mods.slow_axis == 0 */
    if (!((_c2py_slow_axis_info_mods) == (0))) {
        char _c2py_err[256];
        snprintf(_c2py_err, sizeof(_c2py_err), "check failed: mods.slow_axis == 0 (got %ld). Buffer must be C-contiguous (use slow_axis=0 or [][] notation).", (long)(_c2py_slow_axis_info_mods));
        PyErr_SetString(PyExc_ValueError, _c2py_err);
        return NULL;
    }
    /* overload 0 (always) */
    {
        vnorm((const double (*)[3])info_vec->ptr, (double *)info_mods->ptr, info_vec->shape[0]);
        Py_RETURN_NONE;
    }

#ifdef _MSC_VER
__pragma(warning(push))
__pragma(warning(disable:4702)) /* unreachable code */
#endif
    return NULL;
#ifdef _MSC_VER
__pragma(warning(pop))
#endif
}

static PyObject*
_vnorm_wrapper(PyObject *self, PyObject *args)
{
    PyObject *py_vec = NULL;
    PyObject *py_mods = NULL;
    c2py_buf_pin pin_vec;
    c2py_ptr_info info_vec;
    c2py_buf_pin pin_mods;
    c2py_ptr_info info_mods;
    PyObject *ret = NULL;

    if (!PyArg_ParseTuple(args, "OO", &py_vec, &py_mods))
        return NULL;

    memset(&pin_vec.buf, 0, C2PY.pybuffer_size);
    memset(&pin_mods.buf, 0, C2PY.pybuffer_size);

    if (c2py_pin(py_vec, &pin_vec, &info_vec, C2PY_BUF_READ, _acqord_vnorm, 1) == -1)
        return NULL;

    if (c2py_pin(py_mods, &pin_mods, &info_mods, C2PY_BUF_WRITE, _acqord_vnorm, 1) == -1)
        goto cleanup;

    /* restrict check: mods vs vec */
    if ((char*)info_mods.ptr >= (char*)info_vec.ptr && 
        (char*)info_mods.ptr < (char*)info_vec.ptr + info_vec.len) {
        PyErr_SetString(PyExc_ValueError, "buffer aliasing forbidden");
        goto cleanup;
    }
    if ((char*)info_vec.ptr >= (char*)info_mods.ptr && 
        (char*)info_vec.ptr < (char*)info_mods.ptr + info_mods.len) {
        PyErr_SetString(PyExc_ValueError, "buffer aliasing forbidden");
        goto cleanup;
    }

    ret = _vnorm_impl(&info_vec, &info_mods);

cleanup:
    c2py_unpin_buffer(&pin_mods);
    c2py_unpin_buffer(&pin_vec);
    return ret;
}

static PyObject*
_vnorm_fastcall(PyObject *self, PyObject *const *args, Py_ssize_t nargs)
{
    PyObject *py_vec = NULL;
    PyObject *py_mods = NULL;
    c2py_buf_pin pin_vec;
    c2py_ptr_info info_vec;
    c2py_buf_pin pin_mods;
    c2py_ptr_info info_mods;
    PyObject *ret = NULL;

    if (nargs != 2) {
        PyErr_SetString(PyExc_TypeError,
            "vnorm expects 2 arguments");
        return NULL;
    }

    py_vec = args[0];
    py_mods = args[1];

    memset(&pin_vec.buf, 0, C2PY.pybuffer_size);
    memset(&pin_mods.buf, 0, C2PY.pybuffer_size);

    if (c2py_pin(py_vec, &pin_vec, &info_vec, C2PY_BUF_READ, _acqord_vnorm, 1) == -1)
        return NULL;

    if (c2py_pin(py_mods, &pin_mods, &info_mods, C2PY_BUF_WRITE, _acqord_vnorm, 1) == -1)
        goto cleanup;

    /* restrict check: mods vs vec */
    if ((char*)info_mods.ptr >= (char*)info_vec.ptr && 
        (char*)info_mods.ptr < (char*)info_vec.ptr + info_vec.len) {
        PyErr_SetString(PyExc_ValueError, "buffer aliasing forbidden");
        goto cleanup;
    }
    if ((char*)info_vec.ptr >= (char*)info_mods.ptr && 
        (char*)info_vec.ptr < (char*)info_mods.ptr + info_mods.len) {
        PyErr_SetString(PyExc_ValueError, "buffer aliasing forbidden");
        goto cleanup;
    }

    ret = _vnorm_impl(&info_vec, &info_mods);

cleanup:
    c2py_unpin_buffer(&pin_mods);
    c2py_unpin_buffer(&pin_vec);
    return ret;
}


/* -------------------------------------------- */
/* Module definition                          */
/* -------------------------------------------- */

static PyMethodDef _methods_varargs[] = {
    {"vnorm", (PyCFunction)_vnorm_wrapper, METH_VARARGS, "vnorm(vec, mods)\n--\n\nvnorm(vec: buffer, mods: buffer) -> void\n\nParameters\n----------\nvec : buffer\n    Type: float64 (format 'd')\n    Shape: 2D\n    Axis 1: 3 elements\nmods : buffer\n    Type: float64 (format 'd')\n    Writable\n    Shape: 1D\n\nChecks\n------\n  vec.format == 'd'  [ValueError]\n  mods.format == 'd'  [ValueError]\n  vec.ndim == 2  [ValueError]\n  mods.ndim == 1  [ValueError]\n  vec.shape[1] == 3  [ValueError]\n  mods.n == vec.shape[0]  [ValueError]\n  vec.slow_axis == 0  [ValueError]\n  mods.slow_axis == 0  [ValueError]\n\nOverloads\n---------\n  vnorm(const double vec[][3], double mods[], int n)\n    Map: vec = vec.ptr (const double (*)[3])\n         mods = mods.ptr (double *)\n         n = vec.shape[0] (int)"},
    {NULL, NULL, 0, NULL}
};

static PyMethodDef _methods_fastcall[] = {
    {"vnorm", (PyCFunction)_vnorm_fastcall, METH_FASTCALL, "vnorm(vec, mods)\n--\n\nvnorm(vec: buffer, mods: buffer) -> void\n\nParameters\n----------\nvec : buffer\n    Type: float64 (format 'd')\n    Shape: 2D\n    Axis 1: 3 elements\nmods : buffer\n    Type: float64 (format 'd')\n    Writable\n    Shape: 1D\n\nChecks\n------\n  vec.format == 'd'  [ValueError]\n  mods.format == 'd'  [ValueError]\n  vec.ndim == 2  [ValueError]\n  mods.ndim == 1  [ValueError]\n  vec.shape[1] == 3  [ValueError]\n  mods.n == vec.shape[0]  [ValueError]\n  vec.slow_axis == 0  [ValueError]\n  mods.slow_axis == 0  [ValueError]\n\nOverloads\n---------\n  vnorm(const double vec[][3], double mods[], int n)\n    Map: vec = vec.ptr (const double (*)[3])\n         mods = mods.ptr (double *)\n         n = vec.shape[0] (int)"},
    {NULL, NULL, 0, NULL}
};

static PyModuleDef _module_def = {
    PyModuleDef_HEAD_INIT,
    "c2py_vnorm_ndarray",
    "Module: c2py_vnorm_ndarray\nSource: ['tiny_kernel.c']\nHeaders: ['tiny_kernel.h']\nTiming: no\nFree-threading: no (GIL re-enabled on 3.14t)",
    -1,
    NULL,  /* methods set at init */
    NULL, NULL, NULL, NULL
};

static PyModuleDef_FT _module_def_ft = {
    PyModuleDef_HEAD_INIT_FT,
    "c2py_vnorm_ndarray",
    "Module: c2py_vnorm_ndarray\nSource: ['tiny_kernel.c']\nHeaders: ['tiny_kernel.h']\nTiming: no\nFree-threading: no (GIL re-enabled on 3.14t)",
    -1,
    NULL,  /* methods set at init */
    NULL,  /* m_slots = NULL (single-phase init; PyUnstable_Module_SetGIL handles FT) */
    NULL, NULL, NULL
};

C2PY_EXPORT PyObject* PyInit_c2py_vnorm_ndarray(void) {
    if (c2py_runtime_init() != 0) {
        return NULL;  /* Python will raise ImportError */
    }

    PyObject *module = NULL;
    PyMethodDef *methods = C2PY.use_fastcall ? _methods_fastcall : _methods_varargs;

    if (C2PY.is_free_threaded) {
        _module_def_ft.m_methods = methods;
        if (C2PY.Module_Create2 != NULL) {
            module = C2PY.Module_Create2((PyModuleDef*)&_module_def_ft, 3);
        }
    } else {
        _module_def.m_methods = methods;
        if (C2PY.Module_Create2 != NULL) {
            module = C2PY.Module_Create2(&_module_def, 3);
        } else {
            module = C2PY.InitModule_2_7("c2py_vnorm_ndarray", methods);
        }
    }

    if (module != NULL) {
    }
    return module;
}

C2PY_EXPORT void initc2py_vnorm_ndarray(void) {
    c2py_runtime_init();

    PyObject *module = C2PY.InitModule_2_7("c2py_vnorm_ndarray",
        C2PY.use_fastcall ? _methods_fastcall : _methods_varargs);
    if (module != NULL) {
    }
}
