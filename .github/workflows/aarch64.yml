name: aarch64 CI

on:
  push:
  workflow_dispatch:

jobs:
  test-aarch64:
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Show architecture info
        run: |
          uname -m
          gcc --version | head -1
          file /bin/ls

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install c2py23 and test deps
        run: |
          pip install -e . --quiet
          pip install --only-binary=:all: pytest numpy PyYAML setuptools wheel --quiet 2>/dev/null || \
          pip install pytest PyYAML setuptools wheel --quiet

      - name: ABI diagnostic
        run: python tests/abi_list_dlls.py

      - name: Run tests
        run: pytest tests/ -v --durations=10 --tb=short
